<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.hlkw.cafe.repository.BoardMapper">

    <select id="search" resultType="board">
        select * from dust_board
        <if test = 'searchBy.equals("writer")'>
            where id in (select id from dust_member where nickname like '%${word}%')
        </if>
        <if test = 'searchBy.equals("title")'>
            where title like '%${word}%'
        </if>
        <if test = 'searchBy.equals("content")'>
            where content like '%${word}%'
        </if>
    </select>
    <select id="findOne" resultType="board">
        select * from dust_board where board_no = #{boardNo}
    </select>

    <select id="search" resultType="board">
        select * from dust_board where
    </select>

    <update id="modify">
        update dust_board set title = #{title}, content = #{content} where board_no = #{boardNo}
    </update>

    <delete id="remove">
        delete from dust_board where board_no = #{boardNo}
    </delete>

</mapper>